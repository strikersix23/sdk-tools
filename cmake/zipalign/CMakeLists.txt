cmake_minimum_required(VERSION 3.4.1)

project(zipalign)

set(ZIPALIGN ${CMAKE_SOURCE_DIR}/${AOSP})

set(ZIPALIGN_SRC_FILES
	)

set(CMAKE_CXX_FLAGS "${GLOBAL_CXX_FLAGS}")


add_library()
target_link_libraries()
